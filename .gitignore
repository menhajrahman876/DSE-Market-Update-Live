# --- data / secrets: never publish ---
*.xlsb
*.xlsx
*.xlsm
.streamlit/secrets.toml

# --- generator's default output (we commit dashboard_snapshot.html instead) ---
dse_live_dashboard.html

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/

# --- OS / editor / Google Drive junk ---
.DS_Store
Thumbs.db
desktop.ini
*~
*.swp
~$*

# --- maintainer-only local tooling: references this machine's absolute
# paths and drives the push itself, so it stays out of the public repo ---
Refresh Snapshot and Push.bat
watch_and_publish.py
Enable Auto-Publish.ps1
Disable Auto-Publish.bat
.last_synced_mtime
.watch_lock
auto_publish.log
run_watcher_hidden.vbs
