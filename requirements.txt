# Streamlit Community Cloud installs these. The Cloud app (streamlit_app.py)
# only needs streamlit to display the committed snapshot; pyxlsb + numpy are
# here so a fresh clone can also regenerate the snapshot locally with
# `python dse_live_dashboard.py`.
streamlit>=1.49
pyxlsb>=1.0.10
numpy>=1.24
